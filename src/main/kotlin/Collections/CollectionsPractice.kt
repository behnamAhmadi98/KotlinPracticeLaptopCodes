package Collections

/** Kotlin has two types of collections : Mutable and Immutable
 * * 1-Immutable means that the collections are READ-ONLY
 *****List, listOf, and listOf(<T>), set->setOf, map, MapOf are all Immutable.
 *2-Mutable means that they can be read and write.
 *
 * *
 *
 */
fun main() {

}